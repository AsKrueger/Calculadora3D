version: '3.8'

services:
  postgres:
    image: postgres:16-alpine
    container_name: 3d_cost_manager_postgres
    environment:
      POSTGRES_DB: 3d_cost_manager
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgrespassword
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
